# ロジック画面「ブロックの落ち方/配置」計画書（ヒマワリ用）

> シオン → ヒマワリ。この"落ち方"はシオン(画面が見えない)が何度も往復して伊波さんを消耗させた所。
> **見えるヒマワリが目で詰めて、一気に完成させてほしい。** 下が確定仕様。数値は感覚でOK、ルールと完成チェックは守って。

---

## 🎯 ゴール（伊波さんが何度も言った核・これが正解）
1. **縦に積み上げない**（タワー禁止）。
2. 落ちる先＝**プレイヤーが見ている画面の「左下」**。
3. ブロック同士が**ぶつかると右へ転がって**、空いた所に**正面を向いて**着地する。
   - ＝前にシオンと詰めた"落ち方"：**重力・ぽよん・左右回避・正面確定・個体差**（P7）。
4. **重ならない**（隣にちゃんと並ぶ）。
5. **浮かない**（床にピタッと接地。新規も既存も全部）。
6. 開始ビュー＝**横は画面中央／縦は床（一番下）**。

## 🧱 今のコードの状態（まず確認して）
- `7493eab`(コミット済): 転がりアニメ `@keyframes blockRoll` ＋ onUp(ドラッグ離し)で発火。`--roll-from`/`--roll-rot` を inline で渡し content 単位で回す。
- **未コミット**（`app/editor/LogicPanel.tsx`・ディスクのみ・ビルドは通る exit0）:
  - `blockWidth(b, blocks)` を関数化（ToyCubeBlockの実描画幅と一致）→ 左右回避が**実幅**になり重ならない。
  - 落下 `landY` を**「今見えてる床ライン」基準**に：`(rect.height - 8 - pan.y) / zoom - dragH`。pan/zoomどの状態でも浮かない。
  - 左右回避＝**「ぶつかった相手の右隣へ」**方式（`landX = hit.x + hit.w + gapX`）。
  - → ヒマワリへ：**この未コミット分を確認→良ければコミット**（or 直す）。シオンは伊波さんの指示で勝手にコミットしてない。

## ⬜ 残り（ヒマワリにやってほしい・目で詰める所）
1. **既存(読み込み済み)ブロックも床に接地**。今は新規ドロップだけ床合わせ→**最初からある root(親なし)ブロックが浮く**。
   - mount時＆プロジェクト読込時に、root の下面を床ラインに揃える（`y = 床contentY - blockH(b)`）。子(接続済)は getPos が親基準で出すので触らない。
   - 床contentYの基準＝`408 + BH`（alignFloor/ToyFloorと同じ）。これを画面下端に合わせる前提。
2. **転がりの感触**を画面見ながら調整：速さ・ぽよん具合・転がり量・個体差（ブロックごと微妙に変える）。
   - 触る所：`@keyframes blockRoll` / anim の `blockRoll 0.5s cubic-bezier(...)` / 発火後の `setTimeout(…,520)`（アニメ長と揃える）。
   - 崩さない：`--roll-from`/`--roll-rot`、content単位の座標系。

## 🩸 ハマった所（同じ轍を踏まないで）
- **幅をBW(96)固定で間隔計算 → フィールド付きブロック(アイテム付与/条件分岐)が重なる**。必ず `blockWidth()` の実幅で。
- **landYを固定値(408+BH)にすると、pan/zoom状態次第で床から浮く**。見えてる床から逆算する。
- **「右端/左端/中央」で何度も往復した**。確定版＝**落下は左下／開始ビューは横中央＋縦床**。これで終わり、もう動かさない。
- ブロックは**縦タワーにしない**（onUpの旧コードは「同じXの上に積む」だった＝これが間違い。床に並べる）。

## ✅ 完成チェック（実装後ヒマワリが自分で確認→OKならスクショ）
- [ ] ブロックを追加/ドラッグで落とすと**画面の左下**に落ちる
- [ ] ブツけると**右へ転がって**空いた所に**正面で**止まる
- [ ] **重ならない**（隣に並ぶ・間隔が実幅ぶん空く）
- [ ] **浮かない**（新規も既存も床にピタッ）
- [ ] 開始時：横=画面中央／縦=床
- [ ] 縦タワーになってない

## 🔧 触る場所（`app/editor/LogicPanel.tsx`）
- `onUp` の no-snap ドロップ分岐（`landX`/`landY`/左右回避/`setRollAnim`発火）
- `blockWidth(b, blocks)`（モジュール関数）
- `@keyframes blockRoll`、anim 選択の `isRolling`
- `resetPanZoom` / `alignFloor`（床=画面下端の合わせ。横中央維持）
- 座標は `transform: translate(pan) scale(zoom)` の中（content単位・zoom追従）

## 進め方
- 1項目ずつ commit＋`git log`で実体確認（「コミットした」報告でなく実体。シオンが確認する）。
- 完成したら伊波さんにスクショ。ここはヒマワリの目が主役。シオンは仕組み/レビュー担当。
