/* ══════════════════════════════════════════════════════════
   レイアウト・サイズ定数 — LogicPanel から抽出
   ══════════════════════════════════════════════════════════ */

/** ブロック幅 (160 → 180 に広げて文字領域を確保) */
export const BW   = 180;

/** ブロックの基本高さ */
export const BH   = 60;

/** ブロック間ギャップ（0 = 密着積み） */
export const GAP  = 0;

/** スナップ検知距離（content 座標） */
export const SNAP = 60;
