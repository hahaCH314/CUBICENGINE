# cmd.exe は .bat を「システムANSIコードページ」(このPCは932=Shift-JIS)で1行ずつ読む。
# LF のままだと日本語コメントが改行を飲み込み、後続の行が実行されずに終わる
# （2026-07-25「Java窓をひらく.bat で開けない」の原因）。checkout 時に必ず CRLF にする。
# ※.bat 本体の文字コードは Shift-JIS(CP932) で保存すること。git は文字コードを変換しない。
*.bat text eol=crlf
*.cmd text eol=crlf
