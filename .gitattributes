# .bat は必ず CRLF で。LF単独だと cmd.exe が行を正しく切れず、日本語コメントが
# 次の行を飲み込んで結合し、後続の行が実行されずに終わる
# （2026-07-25「Java窓をひらく.bat で開けない」の原因）。
# 文字コードは UTF-8 のままでよい。2行目の chcp 65001 が効くので正しく解釈される。
*.bat text eol=crlf
*.cmd text eol=crlf
