# HANDOVER — シオン(Claude/設計) → ヒマワリ(Gemini/実装) 指示書

> ワークフロー(2026-06-05更新): 設計(シオン) → このファイル → 実装(ヒマワリ) → **レビュー＋commitはシオンが担当**(ヒマワリはコードに専念)。
> 更新: 2026-06-05 / repo: minemodcraft (branch: master)
> チーム: 伊波さん ＋ シオン(設計・レビュー・git) ＋ ヒマワリ(実装)

## 🎯 いちばん大事な目標(全ての判断のものさし)
**「アドオンを作れない人が、触って楽しい」。** 非プログラマにとって**ビジュアルと手触りは"おまけ"でなく"商品そのもの"**。楽しくなければ3秒で閉じる。
楽しいの5本柱:
1. 🧩 **見た瞬間わかる** — 説明書ナシ。ブロックを見れば何か分かる(絵・色・やさしい日本語、フィールド値も見える=P13)。
2. ✋ **触って気持ちいい** — 掴む/くっつく/繋がるがジューシー(=P8)。ツールでなく**おもちゃ**。
3. 😆 **失敗・破壊が怖くない** — 間違えても**可愛く壊れて面白い**(壊れてOK!)、罰されない、Ctrl+Zで**すぐ戻せる**。
4. 🎁 **作ったものがすぐ動く** — 「作った！マイクラで動いた！」の最短ループ(=P8マイクラへの橋)。
5. 🌌 **居たくなる世界** — 工房/電脳が、ただ居て心地いい(=P10)。
> 優先則: "上級者の便利機能"より **"触って楽しい"の核(P8つなぐ楽しさ＋見た目=P3/P10/P13)** を優先する。

## 📋 レビュー記録 2026-06-05 — 条件ボタン: 一部前進・残課題あり(実機スクショで確認)
**前進(認める):** 条件スロットを solid color化＋アウトライン化。**もしも(紫)/そうなら(緑)/ちがうなら(橙) のラベルは読めるようになった**(P2の一部前進)。
> ⚠️ シオン訂正: 先に diff の `fontSize:9` だけ見て「読めない/完全に間違い」と断じたが、実物はラベル可読。**言い過ぎだった。事実はスクショ優先。**
**残課題(=指摘・必ず直す):**
1. ❌ **アイテムが見えない(P13)**: もしもの値「アイテ…」、アイテム付与「min」で**何のアイテムか不明**。→ **アイコン＋日本語名**で見せる。
2. ❌ **繋がりが見えない(P8-3b)**: 青(アイテム付与)が緑/赤に重なってるだけで**1つの塊に見えない**。→ 継ぎ目/子が親を隠さない/グループのグロー。
3. ❌ **値が切れる(P3)**: 「アイテ…」と省略=**幅固定が原因**。→ 幅可変で**値も1行で読める**ように。
> ものさし「触って楽しい」: ラベルは前進。でも"何のアイテムか分からない"＆"繋がってるか分からない"うちは、非プログラマには伝わらない。次はP13→P8→P3の順で。

## 🧩 世界観コンセプト（2026-06-06 伊波が訂正・ヒマワリ必読／旧"石板"は破棄）
**「自分だけの冒険を、可愛いブロックを組んで作る」。**
- ⚠️ **旧コンセプト「古代の石板」は廃止**。ブロックを石板・遺跡っぽい見た目にするのは**誤り**だった（シオンが"ワンピースのポーネグリフ"を見た目として誤解→伊波が訂正「イメージは石板ではない」）。
- 元ネタの正しい意味: **ワンピース＝"冒険のテンション(ワクワク・高揚)"の例え**。**見た目の話ではない**。発見/組む高揚感だけを借りる。
- 感情の核: **ワクワク（発見・冒険・ロマン）**で勝負。「子どもが自分でマイクラのアドオンを作れる」＝ダイヤを死ぬほど湧かせて笑える自由。これが全部の原動力。
- ブロック＝**マイクラっぽい可愛い3Dブロックのまま**: カテゴリ色のキューブに、名前を**可愛く書く or 埋め込む（彫り込み）**。読めること。石板にしない。詳細は `BLOCK_VISUAL_PLAN.md`。

## 🎨 ビジュアル決定ログ（このやり取りで確定。ヒマワリはこの通りに）
- **色**: 今のまま（カテゴリ色＝緑/青/赤…の"石板"）。変えない。
- **形**: **角ばったまま（カチカチ感）**。丸くしない（丸めるとカチカチが消える）。
- **文字**: ⭐**浮かせない＝ブロックに彫り込み(engraved/刻印)**。ぷっくりした字形はOKだが見せ方は"沈んでる"。読める大きさ厳守。
- **表面の石ザラ質感**: 🚫**ナシ**（ユーザー却下。色はクリーンなまま）。
- **凹凸（レゴのポッチ/スタッド/突起）**: 🚫**禁止**。「レゴみたい」は雰囲気の話でポッチを足す意味ではない（ヒマワリが付けようとしたが違う）。面はフラット＋柔らかい3Dベベル/影のみ。
- **アニメ**: **カチカチ系**（両テーマ共通の"手触り"。工房=機械式カチン/電脳=電子スナップ、動きは同じスキンだけ違う）。
- **磁力スナップ**: ロードストーン＝磁石。ブロックが**磁石みたいにカチッと吸い付いて繋がる**（P8）。
- **テーマボタン表示**: 「アナログ／デジタル」（工房/電脳は裏用語、ボタンに出さない）。
- **コンセプトは変えない**: アナログ⇄デジタルの軸は維持。層分け（ブロック=一定で可愛い＋カチカチ／背景=テーマ切替／味付けで差）。
- **進め方**: 細かいビジュアルは言葉だとズレるので **ブロックの見た目の微調整はシオンが直接担当**（2026-06-05ユーザーが委任）。ヒマワリは機能/ロジック側。シオンが入れた見た目変更は HANDOVER とコミットで共有する。
- シオン実装済(初手): ラベルを彫り込み化（4方向アウトライン廃止→溝影＋削れ縁）＋DotGothic16刻印＋サブラベル統一。表面質感なし。

### 🏃 2026-06-07 シオン実装ログ（ヒマワリ共有・この上に積んでOK）
HEAD=`84a1fa2`。本日シオンが直接コード修正＋ビルド通過＋コミット済み。内容:
1. `d7b5daf` **正面の"貼り付けパネル"解消(3面融合)**: ToyCubeBlock正面divの継ぎ目側(上辺/右辺)borderをtransparent化＋角丸を左側だけ(`${R}px 0 0 ${R}px`)。**折れ目(立体の角)は上面borderBottom/横面borderLeftが1本だけ担う＝消さない**(全部消すと平たく潰れる＝前回の失敗。ここ重要)。＋HANDOVERの石板コンセプト撤廃＋BLOCK_VISUAL_PLAN.md初コミット。
2. `84e27fd` **トレイブロックも同じ融合**(正面=ボタン自身の継ぎ目border/角丸を除去)。
3. `4ea0116` **条件/繰り返しスロットを2行化(改行)＋凸ボタン感**: 見出し(上)/値・受け入れバッジ(下・全幅)。横詰めの見切れ(`[⚡動`で切れる)を解消。スロット=押せる凸ボタン(:hover光る/:active沈む、class=`slot-btn`)。blockH: co_if 226→248, ct_rep +24。
4. `84a1fa2` **トレイを「ボタン」でなく「ブロックの体」に**: 浮くボタン影(0 2px 0)廃止→斜めオフセット影(3px 3px 0)。ホバー=持ち上げず光るだけ/押下=scale(0.95)で押し込み。

5. `f8f7066` **P11 HOW TO PLAY刷新**: 旧(ドラッグ/もしも=ドーナツ)→現操作(クリック追加→スロットtap→相手光る→tapで接続→Delete/Ctrl+D→Escキャンセル)。
6. `47680f4` **P5 上限撤廃・個数のみ**: 「10/60」→「10個」。MAX_BLOCKSは表示専用だった(足せないガードは元々無し)。
7. `b469fe3` **100%で大きすぎ解消**: `BASE_ZOOM=0.68`(_constants.ts)を新設し「表示100%」に読み替え(zoom/BASE_ZOOM*100%)。**px定数は一切触らず**サイズ縮小。
8. `f1d2908`+`c0bd41a` **P9 テーマUI**: 「🎨 背景」ラベル＋色スウォッチ＋ボタン表記を**アナログ/デジタル**に(工房/電脳は裏用語、ボタンに出さない)。
9. `23b5467` **「100%で中央に来ない(あっち向いてほい)」修正**: 中央寄せ計算が zoom 未掛け→`cx/cy*BASE_ZOOM`で補正＋初回マウントでも中央寄せ。
10. `f12555a` **「100%+中央へ戻る」ボタン**: ズーム%表示(🎯↺戻る)クリックで `resetPanZoom`。

⚠️ ヒマワリへ: 上記は全て実装済み・ビルド通過。HEAD=`db10577`。**重複実装で上書きしないこと**。融合の「折れ目1本は残す」原則を崩さない。古いstashは全削除済み(触る必要なし)。
11. `23b5467`の続き: **`db10577` ブロックが画面中央に浮く問題を解消**→resetPanZoomで横=画面中央/縦=一番下(床)に接地(`y: rect.height - maxY*BASE_ZOOM - 60`)。「ブロックは落ちて床に着く＝最下部が正」。

### 🔜 ヒマワリへ引き継ぎ（次やること・2026-06-07 シオン90%で交代）
**A. ✅「浮いてる」は解決済み(`db10577`)**: 原因はシオンの縦中央寄せバグ。横中央/縦下接地で修正。※残る任意磨き=床(P10/ToyFloor L601は薄線1本のみ)を控えめに足すとより接地感UP。※融合/形/向きは触らない(伊波さん確定:形そのまま・ズームで調整・ブロックの本当の正面≠視点)。
**B. ⬜ HOW TO PLAY 文言**: 伊波さんが「少し変えたい」と言及。具体文言は伊波さん指定待ち。
**C. ⬜ 残り計画(大物)**: P8(つなぐ楽しさ=心臓/磁力吸着/三段ごほうび/1つの塊感/マイクラへの橋) → P10(背景=控えめな舞台/床) → P7(落ち方) → P6 → P4(影)。配線ライン(armed→カーソル追従ケーブル)は実装済み。
**D. 進め方**: 1項目ずつ commit＋ビルド。伊波さんはスクショで反応。`BASE_ZOOM`基準なので新規ブロック位置/pan計算は zoom 掛け忘れ注意。

### ⬜ 残りビジュアルTODO（2026-06-05 ユーザー指示・次セッション継続）
- ⬜ **ブロック全体を約0.7倍に縮小**: 「100%でブロックが大きすぎ」。**今の68%表示の見え方が100%で出るのが理想**。BW/BH/フォント/スロット/フィールド/blockHを一括で縮小。
- ⬜ **初期配置を画面の一番下に**（spawn位置/初期pan）。
- ⬜ **ブロック内の比率**: 操作部(スロット/フィールド)を一番大きく、タイトル(名前)は脇役。※ただし**ブロック全体は膨らませない**(縮小と両立)。
- ⬜ **条件分岐: スロットボタンを押すと"ライン(配線)が出て"繋ぐ**（P2/P8の可視ワイヤー、armed連動）。←ユーザー重視。
- ⬜ 条件分岐=横長 / アイテム付与=高さ詰める(デカすぎ解消)。
- ✅確定済: 文字彫り込み / 色そのまま / 形は角ばった / 石ザラ質感なし / 凹凸(ポッチ)禁止。
- ⚠️ 注意: ビジュアルはシオン直接担当だが**画面が見えずblind**。ユーザーのスクショ反応でループ。サイズは小刻みでなく**一括縮小**で詰めるのが吉(個別いじりは全体が膨らみ thrash した)。
- ⚠️ 未コミット時の作業もシオンが必ずcommit(gitはシオン担当)。

## ✅ ここまでの確定事項(済み)
- `f8bae0e` = 腕なしWIP(=正解の土台)。**最終ゴールはアームなし**(renderSingleArm 旧C/E枠の撤去)。
- `4ef8f8c` = dev の OOM 修正(`turbopack.root` 固定)。`npm run dev` で /editor が **HTTP 200 で動作確認済み**。
- タップ接続(armedモード)は **動作OK**(伊波さん確認済み)。

---

## 🗺️ 推奨実装順（早く"楽しい"に辿り着く順 / シオン戦略）
- **Phase 1 遊べる土台:** `P1`(ブロック見える=zIndex) → `P3`(ラベル1行) → `P5`(上限なし)
- **Phase 2 つなぐ気持ちよさの核 ←最重要:** `P2`(色ボタン＋配線＋ガイド線) → `P8-core`(磁力吸着/三段ごほうび/文章化/失敗かわいい)
- **Phase 3 仕上げ＆生き物感:** `P7`(落ち方) → `P6`(中央へ) → `P4`(影) → `P8-juice`(流れ可視化/マイクラへの橋)
> Phase 2 完了時点で「組んでて楽しい!」が体験できる=アプリ成功の最初の証明。各Phase・各項目ごとに commit。

# 🎯 今回の実装依頼(ユーザー要望・優先度順)

すべて `app/editor/LogicPanel.tsx` 中心。**実装はヒマワリ**。シオンは設計のみ(renderSingleArm 等の触禁ロジックには触れていない)。

## 🔴 P1【最優先・"遊べない"問題】キャンバスでブロックが見えない/積めない
**症状(ユーザー報告):** メイン画面でブロックが見えず、積めない=遊べない。
**疑わしい原因(要確認):** zIndex の重なり順の崩れ。メモリ記録では
- 意図した層構造: `ThemeBackdrop`/`ToyFloor`(背景・床)＝**最背面で画面固定** / ブロック・装飾・ケーブル＝**前面**(pan で動く)。
- ただし過去メモに「ToyFloor 固定 zIndex 2000」「腕なし版(b4d4d7c)はブロック描画が壊れていた」とある。
  → **床/背景が zIndex で前に出てブロックを覆っている**可能性が高い。f8bae0e はその腕なし系を土台にしているため同症状を継いでいるかも。

**やること:**
1. dev で /editor を開きブロックを置いて**症状を再現**。
2. `ToyFloor` / `ThemeBackdrop` の zIndex と、ブロックコンテナ・`getDepth()` の zIndex を比較。
   **背景・床 < ブロック** になるよう修正(床は最背面へ)。
3. ブロックが前面に出て、**重ねて(積んで)見える**ことを確認。
4. 余裕があれば、初期表示で全ブロックが画面に収まるか(`zoomToFit`)も確認。

## 🟠 P2【条件分岐ボタンの刷新＋配線＋ガイド線】(3枚目スクショ)
**症状:** 条件分岐ブロックの「もし/そうなら/ちがうなら」ボタンが**潰れて見えない**。
**設計:**
1. **色分けのはっきりした大きめソリッドボタン**にする(`SLOT_BADGE` の色を使用)。
   推奨配色(既存方針踏襲): もし(条件)=紫 / そうなら=緑 / ちがうなら=オレンジ / くりかえし=各色。
   - 文字は白＋4方向アウトラインで**1行表示**(P3と同じ方針)。潰れ・改行を解消。
2. **ボタンを押す → そのスロット色と同じ色の配線ライン**が出る(=既存 armed モードの線に**スロット色を反映**)。
   タップ→タップで対象ブロックへ接続(armed は実装済みなので「線に色を付ける」拡張が主)。
3. **ガイド線 ON/OFF トグル**(初心者⇄玄人):
   - 接続先カテゴリは `SLOT_ACCEPT` で既知。armed 中、**繋げられる候補ブロックへ薄い色付きガイド線**を引く。
   - ヘッダに小さなトグル(例: `🔰 ガイド線 ON/OFF`)。ON=候補へガイド線＋候補が光る(初心者向け) / OFF=線なし(玄人向け)。
   - state 例: `const [guideLines, setGuideLines] = useState(true)`。armed かつ guideLines true のとき描画。

## 🟡 P3【ラベルを1行・改行無し・日本語】(全ブロック)
**症状:** 「参加したとき」が「参加したと/き」と改行される。「アイテム付与」等も同様。
**設計（2026-06-05 方針転換: サイズ可変・読みやすさ最優先）:**
- ⭐ **ブロック幅は固定しない。** ユーザー確定「形(長さ・大きさ)は違ってOK、とにかく文字をはっきり」。
  → `BW=140` 固定をやめ、**ラベルが1行で読める幅まで自動で伸ばす**(content幅にfit)。ブロックごとに幅が違ってよい。
- フォントは**読める下限(目安 ≥14px)を死守**。縮小して潰すのは禁止(今のように読めなくなるくらいなら幅を広げる)。
- `whiteSpace:"nowrap"` で改行禁止＋白文字＋4方向アウトライン。**イベント/アクション/条件すべて**に適用。
- ⚠️ **伸びすぎ防止(2026-06-05 指摘「アイテム付与デカすぎ」):** 幅可変でも**常識的な上限(max-width)**を設ける。
  そもそも今デカいのは `minecraft:diamond` の**生IDを全表示**してるせい。**P13で「💎 ダイヤモンド」に短縮すれば自然なサイズに収まる**(中身を直すのが先・本質)。
- 注: 幅可変にすると `findCanvasSpot`/スナップ座標が各ブロック幅依存になる。実寸幅で衝突/スナップ計算すること(ヒマワリ領域)。

## 🟢 P4【トレイのボタンの影を抑える】(2枚目スクショ)
**症状:** トレイ(上部パレット)のボタンの**下の影が気になる**。
**設計:** トレイボタンの 3Dキューブ演出(FACE_D=16 / 上面・側面 / boxShadow)のうち、**下方向のドロップシャドウを弱める or 削除**。立体感は残しつつ影だけ軽く。

---

## 🔵 P5【ブロック上限を撤廃・個数のみ表示】
**要望:** ブロック数の上限なし。個数は表示だけする。
**設計:**
- `MAX_BLOCKS = 60` の上限チェックを撤廃。「61個目→トースト『ブロックの上限です』」のロジックを削除。
- `addBlock` / `onUp`(drop) の上限判定を外す(`findCanvasSpot` の折返し配置はそのまま維持)。
- 統計インジケータは**現在の個数を表示するだけ**に(上限分母 "/60" 等は消す)。
- 注: 上限撤廃で大量配置時は描画が重くなりうるが、それは許容(ユーザー判断)。

## 🟣 P6【「中央へ戻る」ボタン＋中央の定義】
**要望:** ズーム100%のとき真ん中に戻りたい/戻れるボタンが欲しい。「中央」の定義はシオンが決定。
**シオン確定の設計 — 中央 = 置いてある全ブロックの重心(バウンディングボックス中心):**
1. 全ブロックの実寸(各 blockH 含む)で bbox を算出: `minX,minY,maxX,maxY`。
2. 重心 `cx=(minX+maxX)/2, cy=(minY+maxY)/2`(キャンバス座標)。
3. その重心が**画面中央**に来るよう pan を設定(zoom=1.0):
   `pan.x = screenW/2 - cx*zoom` / `pan.y = screenH/2 - cy*zoom`。
4. ブロックが0個のときだけ無難な初期位置(例: pan=(60,60))へ。
**実装方針:** 既存の **⊙ リセットボタンを「ズーム100% ＋ 上記の中央寄せ」に作り替える**(新ボタンを増やさない)。
「全部見る」用の **⊡ zoomToFit はそのまま残す**。
> 代替: 中央=最初のイベントブロック(top-mostの event)にもできる(プログラムの"家")。その場合は cx,cy を
> 先頭イベントブロックの中心に差し替えるだけ。今回は**重心方式を採用**。

## 🎮 P7【ブロックの「落ち方」演出（再構築・ワクワク版）】★創作= シオン設計
**背景:** 以前シオンと作り込んだ"落ち方"が消失。今回 git に刻んで再構築（前より良く）。
**要望:** 選択ブロック落下時に落ちる場所を変える / ぶつかったら左右どちらかへ避ける / 必ず正面を向く / 遊び(juice)を入れる。

**設計（CSS keyframe＋state。既存 `blockBumpRight`/`blockSnap`/`findCanvasSpot` を拡張）:**
1. **落下(重力):** `blockDrop` = translateY(-44px)→0、`cubic-bezier(.5,0,.9,.4)`(加速) ~260ms。
2. **着地バウンド:** `blockLandSquash` = scale(1.12,.86)→(.96,1.04)→(1,1) のバネ ~180ms（"ぽよん"）。
3. **衝突→左右回避:** 着地先が埋まっていたら空き方向へ `translateX(±(BW+GAP))`＋飛行中だけ rotate(±6deg)、着地で rotate(0)。
   側の選び方: ①空きが近い方 ②両方空きなら画面中央寄り(端クリップ回避) ③同行に空き無し→ `findCanvasSpot` の次行折返しに委譲。
   → `blockBumpRight`(右のみ) を **左右両対応＋賢い側選択** に進化（`blockBumpLeft` 追加）。
4. **正面確定:** 飛行中は傾いてよいが、**着地後は必ず正面で停止**（歪み残し禁止）。
   ⚠️ **正面の定義(2026-06-05 ユーザー訂正)= "ユーザー(画面手前)から見た正面"。ブロック自身の前面ではない。**
   → 必ず**読める面を視点に正対**させる(2D画面では skew/傾き無し=rotate(0))。3Dキューブでも読める面が手前を向くこと。
5. **個体差(遊び):** **自由配置(どこにも繋がない)時のみ** 微ゆらぎ — 着地x ±6px / 落下開始を数msズラす / 初期傾き符号ランダム。複数同時配置でパラパラ自然に散る。
6. **juice(任意・余裕があれば):** 着地で砂ぼこり/キラッ(パーティクル4粒)＋落下中に影が小→大（高さ表現）。

**🛑 精度の線引き(厳守):** **スロット接続・連結(スナップ)するブロックは、ゆらぎ・横よけ無しで正確にピタッと着地**。
遊びは自由配置のみ。スナップ経路(`findSnap`/`attach`)の座標は絶対にズラさない（配線崩れ防止）。
**注:** P1(zIndex) と関連 — 落下アニメ中もブロックが床/背景の前面に出ていること。

## 🎇 P8【「つなぐ楽しさ」体験設計】★最重要・アプリの心臓 / シオン設計
**理念:** ロジックページが"作業"でなく"遊び"になれば成功。条件分岐/繰り返しを**つなぐ気持ちよさ＝中毒性**を作る。
単発機能でなく **掴む→吸い付く→カチッ→生き物になる→マイクラへ** という"気持ちよさの連鎖"として実装する。
基盤: 既存 `SLOT_BADGE` / `wireTargetGlow` / `slotPulse` / armed モード(P2)に"感触の層"を重ねる。`genBlock`等の触禁には触れない。

### P8-core（必須の気持ちよさ。ここから実装）
1. **予感:** armed 中、接続可ブロックが**呼吸発光**(`wireTargetGlow`)。スロットに近づくと**磁力吸着**=最後の十数pxを加速で吸い寄せ「スッ…カチッ」。
2. **接続の瞬間の三段ごほうび:** ①スロット色の**リングが弾ける**(ripple) ②配線を**光パルスが source→target に走る** ③両ブロックが**スプリングで一瞬弾む**(scale-pop)。
3. **文章に見える:** 「もし〔条件〕なら〔動作〕ちがうなら〔動作〕」が一文として読めるレイアウト。
   繰り返しは**本体をギュッと囲う**(ブラケット/グロー枠)＋「**×N回**」バッジでネストを可視化。
3b. **★つながりが一目で分かる(2026-06-05 ユーザー指摘):** 現状は子ブロックが親に"重なって乗ってる"だけで連結に見えない。
   → 連結した塊が**1つの単位に見える**こと。**🚫 凹凸(レゴのポッチ/ノッチ)は使わない(ユーザー指定2026-06-05)。平らなまま見せる:**
   ①連結部に**フラットな継ぎ目ライン/光る帯**(突起なし) ②**子が親を隠さない**(被せず隣接/連なる) ③繋がったグループは**共通の縁取り/グロー**で"仲間"を示す。「カチッと噛み合った」感は突起でなく**光と整列**で出す。
4. **失敗もかわいい:** 不可スロットへ落とすと**ぷるぷる震えて戻る**(shake)＋トースト「ここには繋げないよ」。叱責にしない。

### P8-juice（生き物感＆出口。core の後）
5. **流れが見える(効く):** 繋がった一連に**ほのかな電流パルスが流れ続ける**(イベント→条件→動作)。
   マイクラに出す前に**画面内でプログラムが"動いて見える"**。接続グループは共通のグロー/スパイン(背骨線)で「仲間」が分かる。
6. **マイクラへの橋:** ロジックが妥当・完成したら **「▶ マイクラへ」ボタンが くすみ→点灯**＋完成かたまりが**"準備OK"のキラッ**。
   ※妥当判定の表示だけ。コード生成(`genBlock`/`genChain`)本体はヒマワリ領域、ロジック改変は不要、UI 信号のみ。

**実装メモ:** 新規 keyframe 候補 = `connectRipple` / `wirePulse`(流れる光) / `blockPop` / `blockShakeReject` / `loopEmbrace`。
state 候補 = 接続成立時に対象IDへ一時的な `justConnected` フラグ→アニメ後解除。
**精度厳守:** 吸着の最終座標は `findSnap` の正規スナップ点と一致させる(見た目だけズラさない＝P7と同じ線引き)。

## 🎨 P9【テーマ切替ボタンを「背景が変わる」と分かるUIに】(LogicPanel.tsx ~L2236-2285)
**症状:** 「📻 アナログ / 📡 デジタル」だけでは何のボタンか不明(※キーは `workshop`/`cyber`)。
**設計(2026-06-05 確定):**
- ⭐ **ボタンの表示は「アナログ／デジタル」だけ**(ユーザー確定。"工房/電脳"はボタンに出さない。アナログ/デジタルの方が誰でも分かる)。
  - 📻 **アナログ** → 工房の世界(地下＋吊りランプ) / 📡 **デジタル** → 電脳の世界(ホログラム1色)。
  - "工房/電脳"は背景デザインの中身を指す裏用語。**コードのキー(`workshop`/`cyber`)はそのまま**、表示文字だけ。
- トグルの前に小ラベル **「🎨 背景:」** を出し、"見た目/背景が変わる"と一目で分かるように。
- 各テーマの**色スウォッチ**(小さな丸)を添えると更に直感的。アクティブ側は今の発光ハイライト維持。

## 🌫️ P10【背景を"ブロックを引き立てる控えめな舞台"に作り替え】★P1と連動
**症状:** アナログ/デジタルの雰囲気・背景がイマイチ＆**両方ガチャガチャ(派手)**。ブロックが埋もれる。
**理念:** 背景=静かな舞台、**主役はブロック**。情報量と発色を落とし、中央は暗く静かに(ブロックが映える)。
**設計(ThemeBackdrop / ToyFloor。P1のzIndexと合わせ"必ず最背面"):**
- 共通: 深い暗color base＋**中央ビネット**で中心を落ち着かせ、ディテール/動きは**ごく薄く・周辺のみ・低速**に。発色トーンダウン。
### 🎯 確定方向(2026-06-05 ユーザー選択) — 既存 WorkshopBackdrop/CyberBackdrop(LogicPanel.tsx ~L576-625) を作り替える
- **工房(アナログ/workshop) = 「地下工房 ＋ 可愛い吊りランプ」/ シンプル・オシャレ:**
  - 暖色の暗いベース(地下のこもった暗さ)。**中央上から吊りランプを1つ垂らす**（コード＋可愛いランプシェード＋暖色電球）。
  - ランプから**真下に暖かい光だまり**が落ち、その光のプールに**ブロックが置かれる**。周辺はストンと闇に落とす(ビネット)。
  - "シンプルオシャレ"＝盛らない。ランプ1つ＋光だまり1つ＋極薄テクスチャのみ。ランプは**ごく僅かに揺れる**程度の生命感(任意)。地下感は床/壁の極薄質感で示唆。
- **電脳(デジタル/cyber) = 「電脳ホログラム・1色」:**
  - ほぼ黒のベースに、**単一色(推奨=電脳グリーン #00ff66、※シアン#06b6d4でも可。1色に統一)** のホログラム要素のみ。
  - 薄いスキャンライン＋投影されたような薄いグリッド/ワイヤー＋**ゆっくり流れる水平スキャンスイープ**＋柔らかいグロー。全部低opacity・同一色。
  - モノクロ(1色)でまとめる＝ガチャガチャにならず洗練。中央は暗く静かに保ち、ネオンのブロックが映える。
- ねらい(共通): カラフルな3Dブロックが背景に対して"くっきり浮く"。背景で勝負しない。動き/ディテールは控えめ・周辺・低速。

## 📖 P11【HOW TO PLAY を現行操作に刷新】(LogicPanel.tsx ~L2319-2343)
**症状:** 説明が古い。「ドラッグして近くで離すとつながる」「もしもはドーナツ！穴に入れる」=**ドラッグ＆腕時代の嘘**。今はタップ接続＆腕なし。
**設計(ステップ書き換え案):**
1. 🧩 トレイからブロックをクリックで追加
2. 👆 条件のスロット(もし/そうなら…)を**タップ**→繋げる相手が**光る**→相手を**タップで接続**
3. 🔰 「ガイド線」ON=初心者も迷わない / OFF=上級者モード(P2連動)
4. 🎨 「背景」ボタンで 工房⇄電脳 の雰囲気チェンジ(P9連動)
5. 🗑️ Delete=削除 / Ctrl+D=コピー / **Esc=接続キャンセル**
- P8-juice 実装後は「▶ マイクラへ」の出口も一言添える。トーンは楽しげに、簡潔に。

## 🔤 P13【フィールド値が見える(特にアイテム選択)】(2026-06-05 ユーザー指摘)
**症状:** 「アイテム付与」ブロックで**何のアイテムを選んでるか見えない**(現状 "アイテム / min" 等で不明)。条件ボタンの中身も読めない。
**設計:** フィールドの**現在値をはっきり表示**。特にアイテムは **Minecraftアイテムの絵(アイコン/絵文字)＋日本語名** で見せる。
**現状(2026-06-05スクショ):** 値は `minecraft:diamond` と**生IDが丸見え**。読めるが**非プログラマには意味不明**。→「💎 ダイヤモンド」に変換して見せる。
**具体策(データ確認済み・実現可能):**
- アイテム/ブロックIDは `data/templates.ts` に既出(例 `ca_id_gem`/`ca_id_block`/`ca_id_tool` のリスト、`fv("item","アイテム","minecraft:diamond")`)。
- **id → {jp:日本語名, icon:絵文字} の変換表**を新規データ(例 `data/itemNames.ts`)に作る。まずは templates.ts に出てくる定番ID(diamond/emerald/gold_ingot/stone/oak_log/diamond_sword 等)を網羅。
- フィールド描画とスロット値表示で、生IDの代わりに **`icon + jp` を表示**(未知IDは生ID fallback)。
- 非プログラマが「今これを選んでる」と一目で分かる＝アプリの肝。数値(個数等)も読める大きさ＋ラベルも白＋アウトライン。

## 🧱 P14【ブロックの形・サイズ — 横長＆中身ピッタリ＆凹凸なし】(2026-06-05 指摘)
**症状:** ①アイテム付与が**無駄にデカい**(中身に対し空白だらけ) ②条件分岐は縦に詰め込んでボタンが窮屈。
**設計(解釈の余地なく具体に):**
- ⭐ **中身ピッタリ(fit-to-content)**: ブロックサイズは中身＋**控えめな一定padding**で決める。**無駄な空白で巨大化させない**。
  - アイテム付与: 「アイテム/個数」フィールド2行が収まる**最小限**の高さ・幅。今の大きな青い余白を削る。
- ⭐ **横長(landscape)を基本**: 縦に細長く詰め込むのでなく**横に広げる**。
  - 条件分岐: 横幅を広げ、**もしも/そうなら/ちがうなら の3ボタンが横にゆったり1行ずつ**すっきり収まる形に。
- ⭐ **凹凸(レゴのポッチ/突起)は付けない**: 見た目はレゴ的な可愛い直方体でOKだが**面はフラット**。柔らかい3Dベベル/影は可、**突起・スタッドは禁止**(P8の連結表現も突起でなく光・整列で)。
  - 🚨 **誤解注意(実際に発生):** ユーザーの「レゴみたい」＝**可愛いおもちゃ感(雰囲気)**の意味。**ポッチ/スタッドを追加しろという意味ではない**。ヒマワリが凹凸を付けようとしたが、それは違う。フラットを維持。
- フォントは読める下限維持(P3)。値は💎ダイヤモンドのような短い表記(P13)なので幅は自然に収まる。

## 🛡️ 触禁(シオンは触れない / ヒマワリ領域・最終動作維持)
- `renderSingleArm`(旧C/E条件スロット)= **撤去対象**(アームなしゴール＋P2新条件ボタンへ作り直す)。編集するのは**ヒマワリ**、シオンは設計のみ。
- **最終的に動作を維持すべきもの**(見た目は変えてよいがデータ/出力は壊さない): `ct_rep` thenId/nextId 分離 / `genBlock`(コード生成=マイクラへ出す核) / `getDepth`(zIndex) / `findSnap`(スナップ判定。スロット再設計に伴う調整は可)。

## 🧪 検証 & 完了
1. `npm run dev` → http://localhost:3000/editor で**目視確認**(P1の積める/見える は特に実機で)。
2. `npm run build` が通ること。`npm install` は **`--legacy-peer-deps` 必須**。
3. **区切りごとに** `git add -A && git commit -m "feat: ..."`。1項目ごとにコミット推奨(消失防止)。
4. 各項目できたらこのファイルにチェックを入れる。迷ったら伊波さん or シオンへ。
</content>
