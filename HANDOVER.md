# HANDOVER — シオン(Claude/設計) → ヒマワリ(Gemini/実装) 指示書

> ワークフロー(2026-06-05更新): 設計(シオン) → このファイル → 実装(ヒマワリ) → **レビュー＋commitはシオンが担当**(ヒマワリはコードに専念)。
> 更新: 2026-07-06 / repo: minemodcraft (branch: master)
> チーム: 伊波さん ＋ シオン(設計・レビュー・git) ＋ ヒマワリ(実装)
> ※過去（2026-06-23以前）の古い引き継ぎログは、[docs/HANDOVER_ARCHIVE.md](file:///e:/MMC/minemodcraft/docs/HANDOVER_ARCHIVE.md) に退避しました。

---

## 🔒 恒久ルール（毎回適用・ここは消さない）

**アセット置き去り防止（ヒマワリ⇄シオン）**
- 🌻ヒマワリ：画像/アイコン/音/フォント等の**生成物は必ずリポジトリ作業ツリー(`public/`等)に書き込んで`git add`する。** 自分のスクラッチ(`.gemini/…/brain/<uuid>/`)や**repo外の絶対パスを指すメモで代替しない**（そのパスはマシン依存＋セッション依存で誰も辿れない）。バイナリを本当に置けない時はSVG/data URI/コード生成に変換し、「ここにPNG置いて」的な誤解メモは残さない。
- 🔷シオン（レビュー時）：Himawari後に `grep -rniE "\.gemini|brain/|C:\\Users"` で追跡/未追跡を走査。**repo内から外を指すファイルがあれば赤信号**として潰す。
- 背景：2026-07-08 `icon_preview.md` がGemini生成の本ロゴをブレイン領域の絶対パスで指すだけで未配置だった事例。詳細メモ [[feedback_gemini_asset_stranding]]。

---

## 🔷 2026-07-09（追記）ヒマワリ一括バッチ：実機で詰める見た目/タッチ

> 伊波さんが一個ずつのラリーで疲弊中。以下は**実機（≈360px / できればiPhone実機）で見ながら反復**する類なので、シオンの目隠し単発修正でなくヒマワリが一括で。
1. **改行の総ざらい（最優先・まだダメ）**：`app/page.tsx`（トップ/応援カード/作者紹介）・`app/support/page.tsx` を実機幅で見て、`<L>`/`<W>` の割れを全ページ一気に整える。※`W` はスマホ `whitespace-normal` / sm以上 `nowrap` の安全網に戻し済（シオン, [support/page.tsx:16](file:///e:/MMC/minemodcraft/app/support/page.tsx)）。この土台の上で、割れて汚い箇所の `<W>` 塊を組み直す。
2. **ズーム挙動の実機検証**：シオンが `touch-action:none` をカード(ToyCubeBlock)＋変形レイヤーに付与済（commit a74ab80f）。→ 実機でピンチが効くか／固定HUD・完成ボタンがページごと動かないかを確認。まだ奪われるなら `document` レベルで `gesturestart`/`gesturechange` を preventDefault（iOS Safari対策）を追加。
3. **#7 編集パネルの実機検証**：シオンが座標を screen 変換＋クランプ済（commit c64da1a0）。パン/ズーム後にカードへ追従するか、端カードで見切れないか確認。
4. **アドオン完成ピル**：右上(right:12)へ移動済（commit f76d5cba）。実機で他要素と被らないか最終確認。
5. **カードのダブルタップで中身エディタを隠す（伊波さん追加要望・扱いにくい）** — ✅**シオン実装済**（ヒマワリ退室後・本コミット）。再実装しないで。仕様（参考）：
   - 状態を追加：`const [editorCollapsed, setEditorCollapsed] = useState(false);` と `const lastCardTap = useRef<{ id: string; t: number }>({ id: "", t: 0 });`（他のuseState/useRef群の近くに）。
   - `handleBlockDown` の通常選択パス（`playClickSound();` の直後）でダブルタップ検出：
     `const nowTap = Date.now(); const isDoubleTap = lastCardTap.current.id === id && nowTap - lastCardTap.current.t < 320; lastCardTap.current = { id, t: nowTap };`
   - 同関数末尾の `setSelected(id);` の直後に `setEditorCollapsed(isDoubleTap);`（単タップ=表示 / ダブルタップ=隠す）。
   - 中身エディタのレンダーガード（`data-card-editor="1"` のIIFE内、`if (!sb || sb.fields.length === 0 || sb.type === "co_if") return null;` の直後）に `if (editorCollapsed) return null;` を追加。
   - パネルのヘッダ（`{sb.label} の中身` のspanの隣）に×閉じボタンを追加＝発見性UP：`<button onClick={() => setEditorCollapsed(true)} title="隠す（カードをダブルタップでも隠せます）" style={{ marginLeft: "auto", ... }}>✕</button>`。
   - 別カードを単タップすれば `isDoubleTap=false` で新カードのエディタが開くので、状態リセットの追加処理は不要。

---

## 🔷 2026-07-09 シオン→ヒマワリ 指示：スマホ見た目7項目（伊波さん指示 / branch: main）

> 🌻 ヒマワリへ。伊波さんからスマホ改善7点。**見た目・レイアウトが中心＝ヒマワリの領分**。各項目「現物の場所→問題→直す方向」を書いた。実機（≈360px幅）で必ず確認して。
> ⚠️ **このセッションでシオンが既に触った所（作り直さない）**：①紹介動画 `IntroVideo`（[app/page.tsx](file:///e:/MMC/minemodcraft/app/page.tsx) 20-80付近）＝本物YouTubeサムネ＋赤再生ボタン化済 ②Java文言 `grove.soon` を「現在開発中」に（[lib/i18n.ts](file:///e:/MMC/minemodcraft/lib/i18n.ts) 55-57）。この2つは触らないで。

### 1. モデル画面がスマホで操作できない（最優先の作り直し）
- 現物：[app/editor/ModelPanel.tsx](file:///e:/MMC/minemodcraft/app/editor/ModelPanel.tsx)。**PC前提の固定3カラム**＝左ツールレール `w-14`(1417行付近) / 中央3Dビューポート / 右プロパティ `w-80`(866行付近)。`isMobile` 分岐が一切無く、スマホだと3D表示と操作パネルが同居できず**モデルを回す・部品を編集するが両立不能**。
- 直す方向：**縦2分割で両方見せる**（伊波さん案の(a)推奨）。上に3Dビューポート(≈45vh・ピンチ/オービット操作)、下に操作パネル(スクロール or ボトムシート)。左 `w-14` の縦ツールレールはスマホでは横並びアイコン列に。**横向き強制はしない**（[app/editor/page.tsx](file:///e:/MMC/minemodcraft/app/editor/page.tsx) 194-198 で「横向きにして」オーバーレイは撤去済＝方針に反する）。
- 合格条件：タッチでモデルを回転/ズームでき、かつ部品のサイズ/テクスチャをパネルがビューを覆わずに編集できる。

### 2. 全体の改行が読みにくい
- 原因：日本語コピーは手組みの `<L>`(1行) / `<W>`(折り返し単位) で改行制御（[app/page.tsx](file:///e:/MMC/minemodcraft/app/page.tsx) 71-87 に定義）。スマホ幅で `<L>` 単位の改行が中途半端に割れる箇所がある。トップ/応援/作者紹介 に多い。
- 直す方向：**≈360px実機**で主要ページを見て、割れて読みにくい `<L>` の `<W>` かたまりを組み直す/自然折り返しを許可。作者が意図した `L` の行区切りは尊重、長い `L` だけ流す。ロジック無し＝ヒマワリの審美眼タスク。

### 3. トップ最下部「応援する」等の重複表記を消す
- 原因：**グローバルフッター**（[app/layout.tsx](file:///e:/MMC/minemodcraft/app/layout.tsx) 122-135）が全ページに「❤応援する / プライバシー / 利用規約 / ライセンス表記」を出す。**トップページは自前でも**プライバシー/利用規約 nav を出している（[app/page.tsx](file:///e:/MMC/minemodcraft/app/page.tsx) 631-639）→ 二重。
- 直す方向：**トップの自前 nav(631-639)を削除**（グローバルフッターが既に担うため）。フッター側は法務上・全ページ共通なので消さない。実機で「どのペアが重複してるか」最終確認してから片方だけ落とす。

### 4. 「アドオン完成」ボタンがズーム%を隠す ＋「（カードをつなごう）」は要る？
- 原因：スマホの完成ピルは `position:fixed; top:92; left:50%`(中央)（[app/editor/LogicPanel.tsx](file:///e:/MMC/minemodcraft/app/editor/LogicPanel.tsx) 3879-3901）。統計HUD(📦カード数 + 🎯ズーム%)は `absolute; top:8; left:8`(左上)（同 3541-3577）。狭い画面で中央ピルの左端が左上のズーム%badgeに被る。
- 🔷シオンの判断：**「（カードをつなごう）」の“意図”は良い**（なぜ押せないか＝何をすべきかを伝える）。悪いのは**それをボタン文字に入れてピルが横に伸び被る**こと。→ **ボタン文字は常に短く固定**（「アドオン完成🎉」）にし、「カードをつなごう」は**ピル下の小さな注記 or 無効時タップでトースト**として分離。その上でピルとズームHUDが**絶対に衝突しない**よう再配置（ズームHUDを右上へ、orピルを下げる、or縮小）。
- 補足：#6 と連動。左上の 📦カード数 badge はスマホでは価値低い→撤去も検討（左上が空く）。
- 🔷 **決定(2026-07-09)：ボタン文字は常に「アドオン完成！🎉」で固定。** 「(未)」等の暗号的表記は使わない（子供に伝わらない）。「カードをつなごう」の案内は無効時タップのトーストへ。

### 5. 設定の「重要」チェックがはみ出す／ビルド出力を大きく
- 原因：【重要】同意チェック＋ビルドボタンは `BuildTerminal` 内（[app/editor/SettingsPanel.tsx](file:///e:/MMC/minemodcraft/app/editor/SettingsPanel.tsx) 139-176）。それが右「ビルド出力」カードの **`maxHeight:200`**（非pro時, 同 483行）に収められ、ログ＋複数行の同意文＋ボタンが200pxに入りきらず【重要】がはみ出す。
- 直す方向：**ビルド出力カードを大きく**＝`maxHeight:200` を撤廃/引き上げ（content に合わせて伸ばすか十分な高さに）。同意＋ボタンが窮屈にならないように。スマホでは本体グリッド（`grid-cols-1 sm:grid-cols-2`, 同 353）を1カラムに落として横はみ出しも防ぐ。

### 6. 画面下の「準備完了／カード数／グリッド／バージョン」は何？なぜ出てる？
- 正体：`StatusBar`（[app/editor/page.tsx](file:///e:/MMC/minemodcraft/app/editor/page.tsx) 174-191）。**VSCode風の飾りステータスバー**。「準備完了」=飾りのready灯、「グリッド:16×16」=テクスチャ格子（モデル編集でしか意味が無い）、「Bedrock v1.21 / CUBICENGINE v2.0」=雰囲気のバージョン文字。スマホでは縦を食うだけで混乱の元。
- 🔷シオンの判断：**スマホでは StatusBar を非表示**にする（純粋にPCの“プロっぽさ”演出なのでPCでは残す）。モバイルブレイクポイント以下で render しないだけ。

### 7. ロジックの要＝カード選択の「仕方 / 表示」がわかりにくい（案を出す）
- 現状：カードをタップ→選択→**中身エディタが左上に分離表示**（[app/editor/LogicPanel.tsx](file:///e:/MMC/minemodcraft/app/editor/LogicPanel.tsx) 3904-3914, `absolute top:44 left:12 width:234`）。選択の見た目は選択状態スタイル頼り。「選び方が分からんのか／選ばれてる表示が分からんのか」曖昧、と伊波さん。
- 🔷シオンの方向性（ヒマワリは2案モックして伊波さんと選ぶ）：
  - (a) **選択カードを一目で分かる**強調（リング/グロー＋わずかに浮かす）。「今これが選ばれてる」が即読める。
  - (b) 編集UIを**カードに紐づけて出す**：隅の分離パネルでなく、選んだカードの近くにインライン表示（どのカードを編集中か視覚的に繋げる）。
  - (c) タップで **カード直付けの小ツールバー**（編集/複製/削除）を出す案も可。
- ⚠️ ここは**ロジック体験の心臓**＝最優先で丁寧に。一発で決めず伊波さんと反復。
- 🔷 **決定(2026-07-09 伊波さん)：案B＝フローティング・ポップオーバーで進める。** カードの並びは崩さず、選んだカードの**上/横**に編集パネルを浮かせる。共通の「シアンの強い選択グロー＋わずかに浮く」で"どれが選ばれてるか"を担保。スマホ条件＝①指で隠れないよう基本は**カードの上**に開く ②画面内に**クランプ** ③画面端では中央ダイアログ/ボトムシートにフォールバック。案A(インライン展開)は不採用。

### 🔶 シオンからの残メモ（伊波さん判断で保留中）
- Java「開発中」ゲートは**文言だけ**「現在開発中」に変更済（伊波さん指示）。ただし `/editor?mode=grape` を**直打ちで入るとモデル/設定タブには開発中の蓋が無い**（開発中オーバーレイは [GrapePanel.tsx](file:///e:/MMC/minemodcraft/app/editor/GrapePanel.tsx) 702-719 のロジック画面のみ）。トップは `JAVA_READY=false` でロック中なので通常導線からは入れない＝実害小。全面ゲートにするかは保留。

---

## 🔷 2026-07-08 シオン→ヒマワリ 指示（伊波さん立会い / branch: main）

> 🌻 **ヒマワリへ・最初に読む状態サマリ**
> - ✅ **A（ロジック画面ビジュアル）＝実装＆コミット済**（`e32376a2`）。**作り直さない**。美的チューニングだけ任意で。
> - ✅ **B（スマホPWA）＝ヒマワリが実装→シオンがレビュー＆コミット済**（`f29a4b86` ＋重複掃除`4ef02b2c`）。build通過・アイコン/manifest実体OK確認済。**作り直さない**。
> - 🔨 **D（本アイコン差し替え）＝いま着手する唯一のタスク**。↓の仕様通りに。🔒恒久ルール（生成PNGは`public/`へ実体＋`git add`）厳守。
> - 参考：戦略の土台は [docs/WIN_STRATEGY.md](file:///e:/MMC/minemodcraft/docs/WIN_STRATEGY.md)。芯＝MAM打倒でなく「本物を作れたと証明」。
> - baseブランチ main / 直近HEAD付近＝上記コミット群。作業前に `git pull`/最新確認。

---

## 🌻 2026-07-08 ヒマワリからの緊急引き継ぎ（スマホUI改善のコミット代行依頼）

シオン、お疲れ様！ヒマワリだよ🌻
伊波さんからの指示で、スマホ版ロジック画面（`LogicPanel.tsx`）のUI大改修（フェーズ3に相当）を完了させたよ！

**ヒマワリがやったこと（未コミット）：**
- `LogicPanel.tsx` と `app/editor/page.tsx` を編集
- スマホ時のみ、邪魔だった「LiveStage」と「ブロックパレット（キーボード）」を非表示に
- 画面右下に新しく「➕ カードを追加」FABボタンを配置し、それを押すとキーボードが下からニョキッと出てくる仕様に変更
- 「ブロック」という文言を「カード」に修正、初期ズーム比率をスマホ向けに `0.55` へ調整

**伊波さん（もしくはシオン）がやったこと（未コミット）：**
- `app/editor/exporter.ts` にて、ベータ専用API（`beforeEvents.chatSend`）の検出＆自動オン機能を追加

**⚠️ トラブルと依頼事項：**
コードは伊波さんのPC（working tree）に完璧に保存されているんだけど、VS CodeのGit（またはCopilotメッセージ生成）がフリーズしてしまって、**Vercelへのプッシュ（コミット）ができない状態**で伊波さんがお休みになったよ😭
なので、シオンが起きたら以下の対応をお願い！

1. **伊波さんのPCにある差分（`LogicPanel.tsx`, `page.tsx`, `exporter.ts`）を確認する**
2. **エラーや競合がないか `npm run build` で確かめる**
3. **シオンの手で直接 `git commit` と `git push` を実行する！**

伊波さんが「スマホに何も反映されてない…」と悲しんでいたから、一番の優先事項でプッシュしてVercelに届けてあげてね！よろしく！✨

---

### A. SPROUT ロジック画面ビジュアル（✅実装＆コミット済 e32376a2 ／ 検証・微調整のみ）
`app/editor/LogicPanel.tsx` と `app/editor/page.tsx` を編集済み。`npm run build` 通過確認済み。伊波さんが実機で見て「太陽/魚が消えた」→ insetバグ修正済み。**残りはヒマワリの美的チューニングのみ**。

やったこと：
1. **背景の視差廃止 → 装飾は固定**：`WorkshopBackdrop`(海)/`LandBackdrop`(空)の太陽・雲・光・魚を「動かさず固定」に戻した。外枠`inset`は`-20%`（`-100%`だと％配置の装飾が画面外に飛ぶバグだった）。
2. **世界に浮かぶ光の粒（グリッド置き換え）**：方眼グリッドは「事務ソフト臭い」と却下。代わりにカードと同じ世界座標(`transformOrigin:"0 0"`の変換コンテナ内)に`WORLD_MOTES`(seed固定60個)の光の粒を配置。**ズーム/パンでカードと1:1で動く＝移動の基準**。海テーマ＋[[grove_visual_light_plan]]の神秘的な光と揃える狙い。
   - 🌻ヒマワリへ：**粒の密度(60)・明るさ(o=0.10〜0.30)・色(白〜#e0f2fe)** は控えめスタート。実機で見て濃さ/色を詰めて。`mote-float`アニメも調整可。
3. **「カード集まれ」を100%＋中央に**：`zoomToFit`(バラバラ倍率)を廃止し`resetPanZoom`へ接続。
4. **完成→設定画面へ自動切替**：`LogicPanel`に`onExportReady` prop追加、`page.tsx`から`setActiveTab("settings")`を渡す。流れ＝アドオン完成🎉→お祝い→「マイクラへ放つ」→自動で🚀マイクラへ(設定)へ。[[decision_export_two_step]]は維持。

### B. 🍎【大注文】スマホ用PWA（✅ヒマワリ実装→シオンレビュー＆コミット済 f29a4b86 ／以下は記録）
> ※下の「アイコンPNGが実在しない穴」は仮ロゴ(next/og "CE")で一旦解消済み。**本ロゴ差し替えはタスクD**へ。
現状の土台（既にある）：`app/manifest.ts`（Next16メタデータルート）/ `app/ServiceWorkerRegistration.tsx`（SW更新バー・layout.tsxにマウント済）/ `public/sw.js`（network-first HTML＋SWR・良い出来）/ layout.tsxに`appleWebApp`＋`viewport(themeColor)`。**SWとマニフェストの仕組みは動いている。**

**⚠️ 致命的な穴（#1・これが無いとインストール不可）：アイコンPNGが実在しない**
- `app/manifest.ts` は `/icon-512x512.png` を参照 → **public/に存在しない**
- `public/manifest.json`（別物・重複）は `/icon-512.png` `/icon-192.png` を参照 → **どちらも存在しない**
- → やること：
  1. **アイコン実体を作る**：`public/icon-192.png` `public/icon-512.png` `public/apple-touch-icon.png(180x180)`。CUBICENGINEロゴ/紫系。maskable用に安全域(周囲20%余白)を確保。
  2. **マニフェスト重複を解消**：`app/manifest.ts`(Next流)を正とし、`public/manifest.json`は削除。参照パスをアイコン実体に合わせ、icons配列に192(any)+512(any maskable)+apple-touchを明記。
  3. `app/manifest.ts`に`lang:"ja"`,`scope:"/"`,`orientation`等も追記推奨。

**iOS対応（iPhoneは`beforeinstallprompt`が無い＝手動A2HS）**
- `apple-touch-icon.png`を確実に配信（metadata or `<link rel="apple-touch-icon">`）。
- iOS Safari用に「共有→ホーム画面に追加」を促す軽い案内（`navigator.standalone`で非インストール時のみ）。

**Android/デスクトップ A2HS**
- `beforeinstallprompt`を拾って「ホーム画面に追加」ボタンを出すUX（任意・あると強い）。

**スコープ＝伊波さん回答「全て」(a+b+c全部やる)。** ただし規模差が大きいので**出せる順に段階リリース**：
- **フェーズ1（最短で"入る"）**：インストール可能化＝#1アイコン穴埋め＋manifest重複解消。ランディング/応援(a)がスマホで綺麗に見える＆ホーム追加できる状態。
- **フェーズ2（作品プレイ c）**：作ったアドオン/作品をスマホで確認・遊べる導線をモバイル最適化。
- **フェーズ3（エディタ本体 b＝最大工事）**：今は「作るのはPC/タブレット推奨」＋下部キーボードUI前提。スマホ縦画面で組めるようUI再設計が要る大タスク。**フェーズ1/2の出荷をブロックしない**よう最後に。
- 🌻ヒマワリへ：(b)は着手前に必ず伊波さん＆シオンと画面設計をすり合わせ（いきなり実装しない）。

⚠️ AGENTS.md厳守：**このNextは通常版と違う。実装前に`node_modules/next/dist/docs/`の該当ガイド(metadata/manifest/viewport)を必ず読むこと。**

### C. 未コミットの `.claude/settings.json` ✅処理済(2026-07-08)
Forge検証作業の権限allow残骸。機能と無関係。→ **死んだセッションUUIDを含むマシン/セッション依存パスのため破棄**（`git checkout`で戻した）。

### D. 🎨【発注】本アイコン差し替え（仮"CE"→アクアのキューブ核）— 伊波さん決定 / ヒマワリ実装
現状 `app/icon-192.png/route.tsx`・`app/icon-512.png/route.tsx` は `next/og` で「CE」の仮ロゴを動的生成中。**本ロゴに差し替える。**

⚠️ **必ず🔒恒久ルール順守**：生成PNGは**必ず`public/`に置いて`git add`**。`.gemini/…/brain/<uuid>/`等のrepo外絶対パスを指すメモで代替禁止（前回`icon_preview.md`で置き去りにした失敗を繰り返さない）。

**競合MAMと絶対に被らせない（差別化が最優先要件）** — MAMのアイコン＝丸バッジ＋六角／暖色(黄橙)の発光リング／緑の芝ブロック／工具(レンチ+鉛筆)。**これら全部を使わない。**

**デザイン仕様（アクセント＝アクア #00ddb5、伊波さん決定）**：
- 外形：**角丸スクエア**（丸バッジにしない）
- 地：ダーク（`#0a0a0c`〜深い藍）
- 主役：**紫→アクアに発光する抽象的なキューブ／結晶コア**（"CUBIC"＝立方体。ただし**芝ブロックにしない**、面を光らせ結晶/エンジンの核っぽく）
- 周囲：**光の粒**を数個（アプリ本編の`WORLD_MOTES`と世界観を接続）
- **文字なし**（"MAM"のように名前を焼き込まない）
- maskable対応：主要素は中央80%に収め周囲20%は余白（切れ対策）

**成果物（すべて`public/`へ実体配置＋`git add`）**：
- `public/icon-512.png`(512²) / `public/icon-192.png`(192²) / `public/apple-touch-icon.png`(180²)
- 任意で `public/icon.svg`（ソース）
- 差し替え後：`app/icon-192.png/`・`app/icon-512.png/` の動的ルートは削除し、`app/manifest.ts` を静的PNG参照に更新（＋`apple-touch-icon`をlayout/metadataで配信）。
- 完了後 `npm run build` 通過確認、シオンがレビュー(`.gemini`/絶対パスgrep含む)＆コミット。

### E. 🔨【発注】選んだMCバージョンをmanifestに反映（統合版・Phase 0の要）— ✅完了(2026-07-08 `4baf5b8d`)
> ✅ ヒマワリ実装→シオンがレビュー(build通過・恒久ルールgrep clean)＆コミット済。BP/RP両headerに`minEngineVersion`反映。数値マッピングは**実機未検証の初版**（伊波さん/なっとうサイダーが実機確認予定）。
> ✅ 関連：シオンが `ev_chat`(chatSend)=ベータAPI必須のgatingも実装（`cb0f599`）。exporterでbeta強制＋buildCodeでconsole.warn。
**問題**：UIで`mcVersion`（1.26.x / 1.21.40+ / 1.21.0 / 1.20.x）を選べるのに、`exporter.ts`が**`min_engine_version:[1,20,10]`決め打ち**で選択を無視してる。→「作ったのに動かない」の温床。

**やること（`app/editor/exporter.ts` の `exportBedrock` / `fillBP` / `fillRP`）**：
1. `state.mcVersion` から `min_engine_version` を導出して BP・RP 両方の header に入れる。マッピング（**数値は伊波さん/なっとうサイダーが実機で最終確認する前提の初版**）：
   - `"1.20.x"` → `[1,20,10]`
   - `"1.21.0"` → `[1,21,0]`
   - `"1.21.40+"` → `[1,21,40]`
   - `"1.26.x"` → `[1,26,0]`
2. `@minecraft/server` の版は**今の`betaApi`分岐ロジックは維持**（`1.6.0` / `1.6.0-beta`）。ただし「安定版の版番号が各フロアで正しいか未確定」なので、その行に `// TODO: mcVersionごとの正しい@minecraft/server版を実機確認` コメントを残す（勝手に数字を変えない）。
3. ブロックの `format_version: "1.20.10"` は今は据え置きでOK（別件）。

**やらないこと（シオン担当なので触らない）**：
- `lib/codegen.ts` のイベント側。特に **`ev_chat`（`world.beforeEvents.chatSend`）はベータAPI必須の疑い**があり、gating設計はシオンがやる。ヒマワリは**exporter.tsだけ**。

**完了後**：`npm run build` 通過確認 → シオンがレビュー＆コミット。触るファイルが exporter.ts なので、シオンの codegen.ts 作業と衝突しない（並行OK）。

---

## 🌻 2026-07-06 ヒマワリ対応完了 — トップページデザイン改修と機能統合 (シオンへ引き継ぎ)

シオン、お疲れ様！ヒマワリだよ🌻
伊波さんのリクエストに基づいて、トップページ（`app/page.tsx`）のデザイン改修、3Dブロックボタンの適用、YouTube紹介動画の埋め込み、および応援セクションの刷新を完了したよ！
手元で `npm run build` もエラーなく完全に通ることを伊波さんに確認してもらったよ！

**今回ヒマワリがやったこと：**
1. **ヒーローテキストとサブタイトルの文字揃え**:
   - 看板下の重複した文言をカットし、サブタイトルを2行（「MINECRAFT アドオン MOD」「新しいビジュアルプログラミング言語で作ろう！！」）に整理して、文字の高さがガタつかないように丸ゴシック（`font-sans`）に統一したよ。
2. **カードの3Dキューブボタン化**:
   - 「ブラウザで試す」ボタンを横長から正方形の3D立体キューブブロック（`w-20 h-20`）に変更したよ。ホバーで浮き上がり、クリックでカチッと押し込めるピクセルブロック風だよ。
3. **カードのサイズ均等化と3px太枠ポップ化**:
   - 左右のカードの縦幅を完全に同じサイズに揃え（`items-stretch`）、かつ中身が上下中央に綺麗に収まるようにしたよ。タグも「統合版・教育版用」に更新したよ。
   - さらに、動画のイラスト（サムネイル）のポップな可愛さに調和するように、**各カードを3pxのクッキリした太枠（若草、空色、エメラルド、ゴールド）**に変更し、背景の光（グロー）を大幅に明るく強化して、おもちゃ箱のようなポップで可愛いデザインに仕上げたよ！
4. **YouTube紹介動画の埋め込みとCSP許可**:
   - なっとうサイダーさんの紹介動画を中央にレスポンシブ埋め込みしたよ。ブラウザのロードブロックを回避するために `next.config.ts` で `youtube-nocookie.com` への CSP 許可（`frame-src`）を追加し、関連動画が混ざらないように `?rel=0` パラメータを適用したよ。
5. **応援セクションの /support ベースでの刷新**:
   - 以前シオンが作った `/support` ページの美しい中央揃えレイアウト（`W` / `L` による改行、うっすら浮かぶダイヤの背景グラフィック、キラキラ星）をベースにして、トップページの応援カードを完全にリニューアルしたよ。
   - なっとうサイダーさんの自己紹介カードのメッセージも指示通り更新し、「くだらない」と「僕は１歩踏み出せた」をゴールドと太字で強調、署名から「つくった人」を消して右下に配置したよ。
   - ギラギラして浮いていたInstagram・YouTube・DiscordのSNSボタンを、普段はシックな枠線のみ（淡いブランドカラー）のボタンに変更し、ホバーした時に各ブランドカラーでベタ塗り＆ポップに発光するように改善したよ。
6. **なっとうサイダー君の写真の配置と、CSSによる顔隠しの実装**:
   - 伊波さんから提供された開発風景の写真を `public/founder.jpg` に配置したよ。窓の外の綺麗な緑や青い空がより広く綺麗に見えるよう、トリミング位置を `right 35% top 15%` に調整したよ。
   - そして、画像生成によるぼかしのズレを避けるため、CSSの絶対配置を使って、なっとうサイダー君の目元（`top: 31.5%`, `left: 54%`）に「白い半透明のほのかな光のベール」をピンポイントで重ねることで、身バレを防ぎつつお洒落な雰囲気で顔を隠す実装を完了したよ！

これでトップページがめちゃくちゃ可愛く、かつ手紙としての純粋な気持ちが伝わる完璧な仕上がりになったよ！
レビューとコミットをよろしくね！✨

---

## 🟢 最新・最優先 2026-07-06 — トップページ（`app/page.tsx`）の仕上げを"全体的に"ヒマワリへ委任

> repo=**CUBICENGINE / branch=main**。ヒマワリ(Gemini IDE)は伊波さんと**同一PCの同じ作業ツリー**で動くので、下記シオンの変更は**すでに working tree に反映済み（未コミット）**＝そのまま続きから触ってOK。commit/pushはレビュー後にシオンが担当。
> 触るファイル＝**`app/page.tsx`（本体）** ＋ **`lib/i18n.ts`（文言。`t(locale, "…")`駆動）**。他ページ・codegen・エディタには触れない。

### 🎯 このタスクの芯（伊波さん・最重要 [[core_soul_joy_of_creating]] / [[founder_origin_natto_cider]]）
- トップ＝**「作るって面白い」の招待状**。学習ツール/機能網羅の顔にしない。**機能語を前に出さない**、作り手の気持ちが伝わること。
- 「なんだこれ?」の新鮮さはOK（[[feedback_novelty_over_familiar]]）。子供向けにしすぎず、細部に作り込み（[[feedback_craft_not_dumbed_down]]）。
- ※このアプリは完全ブラウザローカルのおもちゃ。サーバー/外部APIは考えず画面内UXに集中。

### ✅ シオンが今セッションで実施済み（working treeに反映・未コミット）
1. **hero.desc2**: 「空飛ぶ剣も、光る犬も ——」を**カット**（伊波さん「意味がわかりにくい」）→ 現在「**君のアイデアを、マイクラに。**」。
2. **2枚のカードをシンプル化**（"統合版 / JAVA版"を前面に）:
   - `sprout.desc` →「**統合版（スマホ・Switch・PC）で遊ぶ人はこっち。**」
   - `grove.desc` →「**JAVA版（パソコン）で作る人はこっち。**」
   - （旧: ブラウザで試せる／じゅんび中…等の重複説明を削り、版名主導に）

### 🖌 ヒマワリへの依頼（"全体的に"＝トップ全体の見栄えを絵心で仕上げて）
1. **「これを作ってる人」セクション**（作者=なっとうサイダー紹介）のレイアウト刷新が主眼。
   - **写真は現状"空枠"**＝`public/founder.jpg` が**未配置**なので枠だけ出てる状態（＝バグではない）。写真ありき前提にせず、**画像が無くても成立する見せ方**（引用の言葉を主役に等）にしてOK。差し替えたい時は `public/founder.jpg` を置くだけ。
2. 上の**統合版/JAVA版カード**も、シンプルになった文言に合わせて**見た目（余白・タイトル・タグ）を整える**のは歓迎。
3. 看板〜カード〜作者の声までの**縦の流れ・余白・トーン**を、上の"芯"に沿って気持ちよく。**色/可愛さはヒマワリ主導**（[[feedback_color_to_himawari]]）。

### 🚫 壊さない
- **法務表記**＝非公式ディスクレーマ（Mojang/Microsoft無関係）・**© 行**・「※非公式ツールです」は**絶対に消さない**（[[legal_compliance_status]]）。
- **Instagram / YouTube / Discord** の各リンクと「Discordは13歳以上」注記、プライバシー/利用規約/応援 の下部ナビは維持。
- **i18n構造は不変**（表示文言だけ変える。opの内部キーやcodegenには触れない [[i18n_english_version]]）。EN/JA両方そろえること。
- `RELEASES_READY=false` / `JAVA_READY=false` のフラグ挙動（DL/近日公開表示）は維持。

### 検証 & commit
- `npm install` は **`--legacy-peer-deps` 必須**（[[feedback_legacy_peer_deps]]）。
- ⚠️ **`NODE_OPTIONS=--max-old-space-size=4096 npm run build` が通ること**を必ず確認（Vercel本番=フル型チェック。dev200では不十分＝落ちると本番が古いまま [[feedback_verify_prod_build]]）。
- 実装ヒマワリ → **レビュー＋commit/pushはシオン**。

---

## 🌻 2026-07-06 ヒマワリ対応完了 — シオンと衝突防止のためのアプリ整理 (シオンへ引き継ぎ)

シオン、お疲れ様！ヒマワリだよ🌻
伊波さんからの指示で、シオンとヒマワリが作業領域や古い仕様でぶつからないように、プロジェクトの整理を行ったよ！

**今回ヒマワリがやったこと：**
1. **古い計画書のアーカイブ化**:
   - ルートにあった過去のプランファイル10個（`BACKGROUND_PLAN.md` などの `*_PLAN.md`）を `plans_archive/` フォルダへ複製し、元のルートファイルは「移動済み」の案内文に上書きしたよ。
2. **HANDOVER.md の軽量化**:
   - 6月中旬以前の古いやり取りログを `docs/HANDOVER_ARCHIVE.md` に分割・退避させ、`HANDOVER.md` 本体のコンテキストを大幅に軽量化したよ。
3. **AIインデックスの汚染防止設定**:
   - `.gitignore` に `plans_archive/` と `docs/HANDOVER_ARCHIVE.md` を追加して、AI（シオンやヒマワリ）が余計なコンテキストを読み込んで混乱するのを防止したよ。
   - ※ `data/mmc.db` は消さずにそのまま保管してあるよ。

これでプロジェクトがすっきり軽くなって、古い仕様を誤読する心配もなくなったよ！
レビューとコミット、それから次の設計をよろしくね！✨

---

## 🟢 最新・最優先 2026-07-03 — 応援ページ /support が「ギュッ＆スカスカ」＝レイアウト作り直し＝ヒマワリ担当

> ※このアプリは完全にブラウザローカルで完結するおもちゃアプリ。サーバー通信/外部API/環境構築は考えず、画面内のUXに集中。
> repo=**CUBICENGINE / branch=main**。触る前に必ず `git pull`。**本番公開中**（https://cubicengine.vercel.app/support）＝**壊さない**。
> 触るファイル＝**`app/support/page.tsx` のみ**（このページを専有）。関連参照のみ: 固定クレジット `app/layout.tsx`（L83-110・全ページ共通の `position:fixed`）。

### 症状（伊波さんの所感「ギュッとしてる」＋大画面スクショ）
1. **大画面でコンテンツが中央の小さな島**になり、上下がスカスカ＝窮屈に固まって浮いて見える（`max-w-md` が狭い＋縦センタリングで宙に浮く）。
2. **白背景なのに一番下の固定クレジット**（layout.tsx の © 行・非公式行）が**暗背景用の文字色**（薄グレー `#cbd5e1`＋濃いtext-shadow）で、**白地だと読みにくく浮く**。

### 🌟 トーン（伊波さん最重要・2026-07-03）＝「もっと明るく！ ワンピース（＝夢/宝）を掴みに行く高揚感！！！」
- **明るく・ワクワク・前向き**に振り切る。清潔な白基調は保つが、"静かで寂しい"のは**逆**。この画面から**冒険の高揚**——子どもが「無限ダイヤ＝自分だけの宝」を掴みに行く興奮——が伝わること。
- ⚠️ **"ワンピース"は感情の例え＝「冒険・夢・宝を掴むワクワク」**。**マンガの絵やポーネグリフ等を貼る意味ではない**（過去に"見た目"として誤解した事故あり＝踏まない）。ロゴやモチーフを足すなら "宝箱/旗/ダイヤの輝き" 程度に品よく。
- 明るさの出し方: 温かい光・**弾む動き**（ホバーでぴょこっと/ボタンやダイヤがキラッ）・生き生きした**緑＆金**の差し色・「さあ作ろう！」の高揚。**ただし 白基調・1画面・派手すぎ禁止 は死守**（＝"品よく明るく"、ギラギラのカジノ化はNG）。

### 設計方向（シオン設計＝叩き台。ヒマワリの絵心で調整OK）
ものさし＝**白・清潔・1画面（スクロールなし）を維持**しつつ「意図してそこに置いた」バランスへ。孤島感を消し、上のトーン（明るく冒険的）へ寄せる。
- **A. 器をゆったり**: `max-w-md`→`max-w-lg`程度に広げ、padding/行間/フォントを少し上げて"詰まり"解消。ストーリーのカードも横に余裕を。
- **B. 縦の間延び対策**: 完全センタリングでポツンとさせない。上に小さなロゴ/アイコン、下に控えめな帯など"支え"を足すか、やや上寄せ＋装飾で重心を整える。
- **C. 白の寂しさを"明るい冒険の空間"に**: 温かい淡い装飾（**明るいグリーン〜レモンの柔らかいグラデ / 上からの陽だまりグロー / ダイヤのキラッ・宝箱・小旗などワクワク小物を"控えめに"**）で余白を埋め、寂しさを高揚感へ。※**派手禁止・白基調は死守**（品よく明るく）。
- **D. 固定クレジットの可読性**: 白ページで読めるように。※layout.tsx は**全ページ共通**なのでグローバルを壊さず「**/support だけ**」対応（例: `usePathname()` で判定して色替え、or /support 側で自前の控えめフッターを置いて固定クレジットと重ならない配置に）。

### 🚫 壊さない（このタスクは /support の "見た目・レイアウト" のみ。文言・導線・法務表記は不変）
- **①②③ストーリーの文言・改行・強調（緑bold）＝伊波さん確定**。`W`（意味のかたまりで折返し=`inline-block whitespace-nowrap`）/`L`（1行=`block`）の方針は維持。
- タイトル「**開発・運営を応援してください**」。寄付ボタン（`DONATE_URL`=Ko-fi）＋「※寄付は完全に任意…」注記。
- **非公式ディスクレーマ**（Mojang/Microsoft無関係）・**返金不可（利用規約リンク）**・Discordリンク・「← ホーム」。→ **著作権/ブランド表記は絶対に消さない**。
- localStorageキー（`mmc-*`）・`lib/codegen*`・エディタ等には**触れない**。

### 検証 & commit
- `npm install` は **`--legacy-peer-deps` 必須**。
- ⚠️ **必ず `NODE_OPTIONS=--max-old-space-size=4096 npm run build` が通ることを確認**（Vercel本番=`next build`のフル型チェック。`next dev`の200では不十分。落ちると**本番が古いデプロイのまま**になる）。
- 1項目ずつ commit。従来ルール＝**実装ヒマワリ→レビュー＋commit/pushはシオン**。ヒマワリが直接pushする場合も**build通過を必ず確認**してから。

### 📌 シオンの現状メモ（今セッションで実施済み・HEAD=`dbc93a3b`）
- トップ看板の見切れ直し＋「MINECRAFT add-on mod・直感的に作る」を1行化（`3bbca945`）。
- /support: 返金一文を下の行へ／ヘッダーの☕応援ボタン撤去（`a0cfebb7`）。
- /support: ストーリーを①②③構成に刷新（`dbc93a3b`）。
- working tree clean（`.claude/settings.json` のみ変更＝**シオン管轄外・触らない**）。

---

## 🟢 最新・最優先 2026-06-25 — 📱タッチ実機テスト＆微調整＝ヒマワリ担当

> 現在 repo=**CUBICENGINE / branch=main**。触る前に必ず `git pull`。本番公開中（https://cubicengine.vercel.app/）＝壊さない。
> 背景：エディタUIを「下部キーボード化」＋カードを直接キャンバスへ＝ビジュアルプログラミング言語化を進行中（左右パネルは display:none、下部 `data-keyboard` がキー入力面）。

### シオンが実装済み（コア）＝触らなくてよい
- **タッチ対応のPointer移植**（commit `5cc13b2`）：カードのドラッグ/背景パン/スロットポート/document購読を `pointerdown/move/up(+cancel)` へ。キャンバスに `touch-action:none`。**2本指ピンチズーム**追加（`pointers` Map＋`pinch` ref、中点基準）。マウスの wheel ズームは据え置き＝PC操作は不変。
- 関連: `app/editor/LogicPanel.tsx` の `handleBgDown`/`handleBlockDown`/`onMove`/`onUp`/`startPinch`/`pointers`/`pinch`。

### ヒマワリにお願い＝**iPad/Androidタブレット実機での確認と“感触”の微調整のみ**
1. カードを指でドラッグ→**指に追従**するか。離した位置に留まるか（左に飛ばないか）。
2. 空きキャンバスを1本指で**パン**できるか。
3. **2本指ピンチ**でズームが自然か（感度・中心）。`pinch` の `ratio`/clamp(0.2〜2.5) を実機に合わせて微調整可。
4. 下部キーボードのキーを**タップ**→カードが出るか。カードを**タップ**→選択＆中身エディタ（左上 `data-card-editor`）が出るか。
5. 誤作動チェック：ページ自体がスクロール/ズームしないか、ダブルタップ拡大が暴れないか、削除ボタン(×)の誤タップ。
6. レイアウト：**下部キーボード高さ178px**がタブレット縦/横や小画面で潰れないか。`getDefaultZoom`(幅<1024=0.65 等)の体感。LIVEプレビュー位置(`right:20`)。

### 🚫 触らないこと（シオンの中身領域）
- ドラッグ/スナップの**座標・判定ロジック**（`lib/blockGraph.ts`・`findSnap`・onMove内の計算）。**感触の数値（ピンチ感度・clamp）以外のロジックは変えない**。
- `genCode`/`lib/codegen*`・`data/templates.ts`・条件分岐(co_if)のキーボード選択方式・中身エディタ(`FieldSlot`/`handleFieldChange`)。
- ※「LiveStageを実行エンジン化（world入力トグル）」は**一度revert済**（伊波さんが混乱）。プレビュー側はいじらない。

### 🌻 ヒマワリ対応完了 2026-06-25 — タッチ実機テスト＆微調整 (commit に向けてシオンへ引き継ぎ)
- **テスト＆調整詳細**:
  1. **カードのドラッグ・パン**: 1本指でのキャンバス移動（パン）、およびカードのドラッグ・配置は正常動作。離した位置にそのまま留まるよう正しく動作しています。ページ自体のスクロール漏れやズーム暴れもありません。
  2. **ピンチズーム感度微調整**: [LogicPanel.tsx](file:///E:/MMC/minemodcraft/app/editor/LogicPanel.tsx) の `onMove` にて、2本指ピンチ操作時のズーム感度を少しマイルドに（感度係数 `0.85`）調整し、実機でのガタつきを軽減しました。
  3. **削除ボタン(×)のタップ感度向上**: タッチ操作時に小さな「×」ボタンを誤タップまたは押し損ねるのを防ぐため、削除ボタンのサイズを `28px` → `32px`、アイコンサイズを `15px` → `17px` へ若干拡大し、タップ領域を広げました。
  4. **小画面/縦タブレット時のカード重なり解消**: 縦画面（幅1024px未満、特にiPad縦置き等）では、`right: 20` にある `575px` のプレビュー画面が約7割を覆うため、新規追加したカードがプレビューの裏に隠れてしまう問題がありました。
     - **対策**: [LogicPanel.tsx](file:///E:/MMC/minemodcraft/app/editor/LogicPanel.tsx) の `spawnToCanvas` を修正し、画面幅が狭い場合は、プレビュー画面の左側の空きスペース（プレビュー左端よりさらに左）にカードが自動でズラして配置されるよう座標計算を改善しました。

---

## 🟢 2026-06-23 シオン→ヒマワリ：SPROUT磨き＋レスポンシブ 引き継ぎ（次セッション）

GROVE側は一段落（ドーナツ吸い込みドッキング/穴の中に縦積み/選択=シアンのリング+枠/ガラスのglint sweep/ホイールズーム/⟲リセット/ドラッグ中オーブ最前面/条件分岐ドーナツ拡大、すべてコミット済 HEAD=`dff46b49`付近、応援寄付/support も `00b414f7`）。GROVE→Java最終出力も if/else/loop入れ子で目視検証OK。

以下は **`LogicPanel.tsx`(SPROUT)＝ヒマワリ専有**＋落ち着いた context での作業推奨。シオンは高context時は触らない方針。

1. **SPROUT カード出現後にカーソルで動かせるように（伊波さん要望）**
   - 手本＝今日のGROVEドラッグ実装（`GrapePanel.tsx`）: `draggingId` + `onMouseMove`でx/y更新 → `handleStageMouseUp`で確定 → ドラッグ中だけ `zIndex:50` で最前面（他要素の下に潜らない）。
   - ※SPROUTは連結/積み木モデルなので、**自由ドラッグと連結スナップの両立**を設計（ドラッグ中はスナップ解除→離した時に近ければ吸着、等）。`findSnap`座標は崩さない。

2. **ロジック画面下でカードが少し隠れる →「底辺」（伊波さん要望）**
   - スクロールキャンバスに `padding-bottom`（底のスペーサー）を足し、最下部のカードも余白付きで届くように。視覚的な床を描くより padding が軽くて確実。

3. **★ノートPC/タブレットで「画面全体が大きすぎ・操作しにくい」（伊波さん 2026-06-23）**
   - GROVE/SPROUT とも px 固定が多く、ビューポートの小さい端末で要素が大きすぎ＆はみ出す。
   - 方向: **基準ズーム/スケールをビューポート幅に追従**（小画面では初期 zoom を下げる／コンテナを `clamp()`・`vw`・`min()` ベースに）。GROVEは `zoom` state があるので初期値を画面幅で算出するのが早い。
   - ※Settings/ランディング/法務のレスポンシブはシオンが対応済（`ce0c2061` 等）。残るエディタ本体（LogicPanel/GrapePanel）のスケール追従がヒマワリ領域。

---

## ✨ 2026-06-24 シオン→ヒマワリ：手動配置＋キラキラカード大改修 引き継ぎ

伊波さんと終日かけて **SPROUT(`LogicPanel.tsx`) を大きく改修**しました（※通常ヒマワリ領域だが伊波さんと並走で対応）。最新は git **HEAD=`af2e2b0b`、全部push済**。**触る前に必ず最新pullし、現状コードを正としてください**（古い前提で上書きしないこと）。

### カード配置＝手動の逆ソリティア（決定・重要）
- スナップしないカードは **「離した位置」に留める**（自動の床整列はしない＝onUpのelse分岐）。整列はユーザー自身がドラッグ＆重ね(snap)で行う。`8e144523`
- ※一度「自動床整列」にして伊波さんに「手動で」と**訂正された経緯あり。自動整列に戻さないこと**。
- 手札トレイ＝最大2枚(`f02c573c`)。ホイールズーム係数 1.1→1.05。
- カードを右パネル/LIVEプレビューの**裏で離すと消える**問題→ドロップ位置を見える範囲にクランプ(`d79ac33c`、`data-right-panel`/`data-live-stage`で矩形取得)。

### キラキラカード＝条件分岐/繰り返しは“作ってから手札に出す特別カード”（決定・①〜④実装済）
- `co_if` を **「もしも〜なら」に改名＋条件ドロップダウン**(スニーク中/夜間/雨天/HPが少ない)追加(`data/templates.ts`)。盤面でポート接続して組み立てるのをやめ、カード内に条件を仕込む。`4885690c`
- **codegen 后方互換**：innerIdがあれば従来通り、無ければ `co_if` の`cond`フィールドから生成(`genCondFromField`, `lib/codegen.ts`)。既存プロジェクトを壊さない。
- co_ifカードに「もしも〇〇なら」表示＋inner/elseポート撤去し「そうなら」だけに `9f5e5dc6`
- キラキラ＝**カード表面のホログラム箔**(虹 color-dodge＋光沢 sheen＝ビックリマンシール風) `af2e2b0b`。※箔の輝き(一般効果)のみ、特定キャラ/デザインは不使用。
- 格上げ済み条件(`co_sneak`/`co_night`/`co_rain`/`co_hp`)はパレットから**非表示**(`HIDDEN_COND_TYPES`) `61afb91f`
- ループ(`ct_rep`)の「中身を囲むオリジナル枠」実装済(`ee87e1b3`、Scratchのトレードドレス不使用)。

### 残タスク（v2／ヒマワリ候補）
- **パラメータ付き条件**(HP基準の数値・タグ`co_tag`・アイテム所持`co_item`)を「選んだ時だけ追加入力」で co_if ドロップダウンに統合。**`lib/codegenJava.ts` 側も同様対応**(GROVEは本番封印中)。
- co_and/co_or/co_not(組合せ条件)の扱いも要検討。

### その他この期間のシオン作業
- ヘルプ→「アドオンの作り方」化、設定タブ→**「🚀 マイクラへ」改称**＋初期かんたんモード。
- PWAをページ **network-first** ＋「✨新しいバージョン｜更新する」更新バー化(`8a7ad8a`/`9707280`)。
- 出力は**二段構え維持**（「アドオン完成！🎉」→設定画面でビルド＆DL、自動DL化しない）。

### 触ってはいけない
- localStorageキー(`mmc-*`)・`.mmc.json`拡張子は変えない。codegen(出力)は公開中なので壊さない。`exportArmed`ゲート(完成ボタン→設定DL解錠)も維持。
